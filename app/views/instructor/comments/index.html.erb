<div class="row mt-3">
   <%= render 'instructor/schedule', today_schedules: @today_schedules %>
 <div class="col-9 mt-2">


<h6 class="d-inline-flex justify-content-start p-1 bg-success text-dark bg-opacity-50">
<%= @comments_top %>
</h6>
  <table class="table" style="font-size: 85%;">
    <thead>
     <tr>
      <th scope="col">日時</th>
      <th sclpe="col">受講者名</th>
      <th scope="col">段階</th>
      <th scope="col">コメント者</th>
      <th scope="col">内容</th>
     </tr>
    </thead>
 <% @comments.each do |comment| %>
    <tbody>
     <td><%= l comment.created_at %></td>
     <td>
      <% @course_id = comment.course.id %>
      <%= link_to comment.course_name, course_path(@course_id) %>
     </td>
     <td><%= comment.howmany %></td>
     <td><%= comment.instructor.name %></td>
     <td><%= comment.comment_body %></td>
    </tbody>
 <% end %>
  </table>
<%= paginate @comments %>